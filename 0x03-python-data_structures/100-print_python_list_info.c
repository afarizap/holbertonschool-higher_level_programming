#include <stdio.h>
#include <stdlib.h>

/**
 * print_python_list_info - prints some basic info about Python lists
 * @p: no idea yet :v
 */
void print_python_list_info(PyObject *p)
{
	printf("[*] Size of the Python List = %d", len);
	printf("[*] Allocated = %d", len);
	printf("Element %d: %s", i, type);
}
